History posts are now clickable. Select any sent post and it opens in the editor — same fields, same draft view.

Test first: click → EditPostView loads with post data → RED. Wired up the handler → GREEN.

Your post history isn't read-only.

📮 postlane.dev