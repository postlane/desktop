History posts are now clickable. Select any sent post — it opens in the editor with everything loaded.

Test first: assert click triggers EditPostView with post data → RED. Wired the handler → GREEN.

📮 postlane.dev