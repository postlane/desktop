History posts are now clickable in Postlane.

Select any sent post from the history panel — it opens in the editor with all the original fields loaded. Edit it, re-draft, re-send.

The test came first: simulate a click on a history post, assert EditPostView mounts with the correct post data → RED. Wired up the click handler → GREEN.

Your post history is now a draft queue.

📮 postlane.dev